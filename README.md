# osint-source-audit
A CLI tool that audits public data sources defined in a YAML config and produces structured audit reports. The tool checks availability, HTTP status, response time, headers (e.g., Last-Modified if present), and validates sample responses against an expected schema.
